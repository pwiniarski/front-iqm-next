import Ink from 'react-ink';

const Papper = ({background = null,style = null}) => (
    <div className="papper-ink">
        {/* <Ink {...{background, style}} /> */}
    </div>  
)

export default Papper;
